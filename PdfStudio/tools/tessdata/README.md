# Tesseract 言語データ (tessdata) フォルダ

このフォルダは将来の OCR 機能（v0.5以降）で使用される予定です。

現状の v0.4 / v0.5.4 では OCR 機能は実装されていません。

将来 OCR 機能が実装されたら、このフォルダに `.traineddata` ファイル
（例: `jpn.traineddata`、`eng.traineddata`）を配置することで OCR が
使えるようになる予定です。

## ダウンロード先（将来用）

https://github.com/tesseract-ocr/tessdata
